<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data noupdate="0">
        <record id="ir_cron_history_project_management" model="ir.cron">
            <field name="name">History Project Management</field>
            <field name="model_id" ref="model_project_management_history" />
            <field name="state">code</field>
            <field name="code">model.cron_create_history_project_management_by_month()</field>
            <field name="user_id" ref="base.user_root" />
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <field name="numbercall">-1</field>
            <!-- <field
                name="nextcall"
                eval="(datetime(year = DateTime.now().year, month = DateTime.now().month + 1, day = 1, hour = 0, minute = 0, second = 6)).strftime('%Y-%m-%d 00:00:06')"
            /> -->
            <field name="nextcall" eval="(DateTime.now() + relativedelta(day=1, months=1)).strftime('%Y-%m-%d 00:00:06')" />
            <field name="doall" eval="False" />
        </record>
    </data>
</odoo>
